// Stub — transaction log with health score history chart
export default function History() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold text-gray-900">History</h1>
      <p className="text-gray-500">
        TODO: Show transaction log (paginated) + a Recharts LineChart of health score over time
        from <code className="bg-gray-100 px-1 rounded">useHealthStore().history</code>.
      </p>
    </div>
  );
}
