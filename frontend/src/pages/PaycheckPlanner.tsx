// Stub — render a timeline of income events vs bill due dates
export default function PaycheckPlanner() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold text-gray-900">Paycheck Planner</h1>
      <p className="text-gray-500">
        TODO: Fetch income sources + bills. Render a horizontal timeline showing income arrivals
        and bill due dates, so the user can see what's due before each paycheck.
        Use Recharts <code className="bg-gray-100 px-1 rounded">ComposedChart</code> or a custom SVG timeline.
      </p>
    </div>
  );
}
